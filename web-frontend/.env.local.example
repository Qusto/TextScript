# AICODE-NOTE: Frontend environment variables for build time
# These variables must be set during Docker build for production deployment

# API URL for backend communication
# For local development: http://localhost:8000
# For Docker deployment: use the public IP/domain of your server
# Example for server deployment: http://192.168.0.24:8000
NEXT_PUBLIC_API_URL=http://localhost:8000
